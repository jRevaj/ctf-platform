# Memcached configuration file
# Intentionally vulnerable configuration

# Basic settings
-m 64            # Use 64MB of memory
-p 11211         # Default port
-u nobody        # Run as user nobody
-l 0.0.0.0       # Listen on all interfaces (vulnerable)
-c 1024          # Allow 1024 simultaneous connections
-v               # Verbose mode

# No authentication required (vulnerable)
# No SASL authentication enabled

# No firewall restrictions (vulnerable)
# No TLS/SSL encryption (vulnerable) 