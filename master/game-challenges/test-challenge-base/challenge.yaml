name: test-challenge-test-challenge-base
title: Base SSH Container
description: Basic SSH-enabled container for testing ssh config

containers:
  target1:
    name: "target1"
    services:
      - name: "SSH"
        port: 22